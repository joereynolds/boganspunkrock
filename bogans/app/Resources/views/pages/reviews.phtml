<section>
    <h1>Reviews</h1>

    {% for review in reviews %}
        <blockquote>
            {{ review.title }}
            <div class="review-source"> <a href="{{ review.url }}">{{ review.source }}</a></div>
        </blockquote>
    {% endfor %}

</section>
