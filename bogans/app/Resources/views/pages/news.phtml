<section>
    <h1>News</h1>

    {% for article in articles %}
        <article>
            <h1>
                {{ article.title }}
                <time>{{ article.date | date('l dS F Y') }}</time>
            </h1>
            <div class="grid">
                <div class="col-4-12">
                    <img src="/assets/{{ article.image }}">
                </div>
                <div class="col-8-12">
                    {{ article.text | nl2br }}
                </div>
            </div>
        </article>
    {% endfor %}

</section>
