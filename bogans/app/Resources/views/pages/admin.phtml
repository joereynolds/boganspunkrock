<section>
    <h1>Gigs</h1>
    {% for gig in gigs %}
        <div><a href="/admin/gigs/{{ gig.id }}">{{ gig.location }}</a></div>
    {% endfor %}

    <h2>Add a new gig</h2>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
</section>

